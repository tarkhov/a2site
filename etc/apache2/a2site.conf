<VirtualHost *:80>
    ServerName ${SITE_NAME}
    ServerAlias ${SITE_NAME}

    DocumentRoot ${SITE_DIR}

    <Directory ${SITE_DIR}>
        AllowOverride All
        Require all granted
    </Directory>
</VirtualHost>
